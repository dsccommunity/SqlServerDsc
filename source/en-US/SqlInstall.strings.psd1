<#
    .SYNOPSIS
        The localized resource strings in English (en-US) for the
        resource SqlPermission.
#>

ConvertFrom-StringData @'
    ## Strings overrides for the ResourceBase's default strings.
    # None

    ## Strings directly used by the derived class SqlInstall.
    EvaluateInstanceFeatures = Evaluate the installed features on the instance '{0}'. (SI0001)
    SetupExecutablePath = The path to the Microsoft SQL Server setup executable '{0}' with product version {1}.
'@
