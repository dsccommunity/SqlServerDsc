@{
    Path = 'SqlServerDsc.Common.psd1'
    CopyDirectories = @(
        'en-US'
        'sv-SE'
    )
}
# Waiting for ModuleBuilder to do away with this file
# when all parameters are provided to the function
